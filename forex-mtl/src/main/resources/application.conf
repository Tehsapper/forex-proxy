app {
  http {
    host = "0.0.0.0"
    port = 8080
    timeout = 10 seconds
  }
  one-frame {
    api {
        base-url = "http://localhost:8081"
        base-url = ${?ONE_FRAME_BASE_URL}
        token = ${ONE_FRAME_TOKEN}
    }
    cache {
      refresh-period = 5 minutes
    }
  }
}
